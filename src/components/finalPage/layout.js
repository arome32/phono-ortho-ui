import React from 'react';
import FinalPageContainer from './container';

export class FinalPageLayout extends React.Component {
    render() {
        return (
          <FinalPageContainer/>
        );
    }
}