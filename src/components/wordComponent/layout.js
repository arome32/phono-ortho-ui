import React from 'react';
import WordComponentContainer from './container';

export class WordComponentLayout extends React.Component {
    render() {
        return (
          <WordComponentContainer />
        );
    }
}